# Historical Random Start - Alpha 6 Method — Current Situational Awareness

Date: 2026-08-26

Superseding active-lane update, 2026-08-26: Current development authority is
the self-contained `0.0.5-alpha-tech-demo` lane named in
`n0001.md`. The peer `0.0.1-alpha` Nexus-review lane
is frozen. The established manager/launcher and release-runtime source exist;
the older statements below that say no functional bridge source exists are
historical and superseded. Retained build output predates this conversion and
is not a current `0.0.5` binary or package. The required non-live suites now
pass 271/271 checks in each supported host, with the Windows PowerShell legacy
long-path caveat recorded in
`n0005.md`. The next action is owner
selection of one bounded pipeline feature for contract definition. Live
deployment, game launch, publication, and deferred features remain outside
this authorization.

Project state: Unpublished independent mod project; first successful Phase 1A
atomic-semantic relocation observed; persistence reload pending

Player-facing product name: `Historical Random Start - Alpha 6 Method`

Current phase: Alpha core execution READY; Phase 1A persistence verification
and bounded launcher/runtime bridge implementation are next; player-preview
0.0.7 remains the frozen visual baseline

Superseding continuity update, 2026-08-21: Phase 1 observation completed and
the Phase 1A relocation probe progressed through multiple documented,
recoverable iterations. The successful exact-target run used Navezgane /
`HRS_Phase1A_Test_001` (with the `A`) and emitted
`RELOC_PLACEMENT_DEFERRED`, `RELOC_PLACEMENT_CALLED`,
`RELOC_SEMANTIC_UNCHANGED`, and `RELOC_COMPLETED` in order. The owner visually
confirmed the relocated first spawn and exited normally. The atomic claim is a
single-callback semantic observation boundary, not a thread lock, transaction,
game freeze, or timing guess. The current required runtime follow-up is a
read-only reload proving marker persistence and no second selection/placement.

The successful mechanism does not use Developer Mode, console commands,
teleport commands, Harmony patching, or custom chunk creation. The user starts
the game through the direct-user-only `Launch Game` handoff and the normal
non-EAC lane; the mod does not alter EAC. AI and automation cannot activate the
launch control.
The lab DLL remains a private pinned proof instrument, not a release artifact.

Owner EAC decision, 2026-08-21: the product may check and report whether its
approved non-EAC runtime lane is positively confirmed, but it must never
disable, lower, configure, bypass, patch, or stop EAC. The frontend states the
requirement before manual launch. Active, unknown, or missing runtime evidence
fails closed with no Random reservation or movement and instructs the owner to
exit and independently choose Steam's normal non-EAC option.

Capsule ownership decision, 2026-08-21: treat Historical Random Start as one
manifest-owned solution capsule. The capsule owns only its explicitly listed
tooling, policy/result records, runtime payload, and recovery metadata. The
game, Steam, shared Harmony/runtime layers, saves/worlds, other mods, and other
Bit Wrecked solutions are external and never installed, modified, or removed
by this capsule. `Remove from Game` is available under Details when the game is
closed, removes only verified deployed game artifacts, and reports `Historical
Random Start removed from game` without claiming a global vanilla reset. The
portable manager, notes, history, and audit evidence remain; no Windows system
files are install or removal targets.

Coexistence decision, 2026-08-21: Phase 1A did not patch Harmony and the exact
run preserved the measured game-managed and foreign-mod aggregates, but this is
not universal compatibility evidence. Install/update checks the bounded capsule
collision surface and refuses unknown owned-folder drift with `Existing
installation requires review`. Harmony, shared runtime layers, and unrelated
mods remain read-only/external and are never repaired, overwritten, disabled,
reordered, or removed by this solution.

Launcher bridge decision, 2026-08-21: keep target scope independent from the
fixed runtime trigger. Planned scopes are exact new-game name, one discovered
game, and eventually all games. Under every scope, runtime may act only for a
genuinely new player's first eligible arrival, at most once per player/game.
Existing players are never moved on load, reconnect, death, respawn, or
continued play. The first bridge implements only an exact typed/copyable New
Game name in the proven local single-player lane. `DiscoveredGame` mutation,
`AllGames`, and multiplayer/server enablement remain future gated work.

Alpha UX and recovery decision, 2026-08-21: capability does not imply
permission. `Launch Game` is always visible but requires Steam running and an
immediate click by the local person who opened the manager; AI, timers, and
automation cannot activate it. If the game is running, all capsule-changing
actions are blocked without queuing, while read-only views remain available.
All operation attempts enter local correlated history. Recovery retains Default
plus five recent snapshot attempts; failed or stale attempts remain visible,
but only immediately revalidated known-good configurations can be restored.
There is no telemetry, watchdog, background service, or crash-time action.
Essential state remains in the upper status area; concise accessible tooltips
explain unfamiliar controls without imitating another platform's visual style.

The older dated paragraphs below remain chronology. Where they describe Phase
1A as unopened, observation-only, or not yet built, this 2026-08-21 update and
the Current Next Action supersede them.

Manifest convergence update, 2026-08-21: the main
`n0008.md` has been audited and revised
against the successful Phase 1A evidence, whitepaper, canonical `PEG-*` grid,
UX contract, official modding context, and this handoff. It is again current as
the product/safety authority; the main plan remains the detailed execution and
canonical decision-grid authority.

Phase 1 build update, 2026-08-20: the exact private target and absence baseline
validate, the observation-only source has been reviewed, and two amended
Roslyn builds are byte-identical. The staged DLL and two-file payload remain
external and preserved. With separate owner authorization, the exact two-file
payload was copied into the new owned DEV Mod folder and its hashes and scan
passed. The game has not been launched and the DLL has not been loaded; first
load retains a separate owner gate. See
`evidence/n0024.md` and
`evidence/n0031.md`.

First-load update, 2026-08-20: the authenticated EAC-disabled NewGame case
passed. The probe reported the exact authoritative local `NewGame` event with
an entity resolved and no mismatch/internal error. The player exited normally.
This proves only the narrow NewGame observation row; reload, death, reconnect,
duplicate, removal, and complete before/after state evidence remain pending.
See `evidence/n0029.md`.

LoadedGame update, 2026-08-20: reloading the exact disposable target produced
the authoritative local `LoadedGame` event and the required
`OBS_LIFECYCLE_REJECTED` outcome. Payload/global hashes remained stable and no
existing world file changed. Death, reconnect/duplicate, and clean removal
remain pending. See
`evidence/n0027.md`.

Died update, 2026-08-20: one ordinary death and respawn in the disposable
target produced the authoritative local `Died` event and required
`OBS_LIFECYCLE_REJECTED` outcome. Payload/global hashes remained stable and no
existing save changed. Duplicate suppression and clean removal remain pending.

Duplicate update, 2026-08-20: two ordinary deaths in one process produced one
normal `Died` rejection followed by `OBS_DUPLICATE_SUPPRESSED` for the repeated
world/entity/lifecycle key. The bounded count did not increase. Clean removal
and a no-probe reload remain pending; multiplayer lanes remain unproven.

Removal/final review update, 2026-08-20: the exact owned probe folder was moved
intact to external recovery and a normal no-probe reload produced zero HRS log
lines. The local single-player observation question passes. Phase 1A is open
only for restricted source/test-packet design; no mutating artifact is approved
for build, install, or execution. Multiplayer and semantic state-delta gaps
remain explicitly unproven. See
`evidence/n0030.md`.

Phase 0A status: Static build/API evidence, source/build design, dependency
record, target schema, and pure fixtures prepared on 2026-08-14; the amended
target-class suite passes 66/66 pure checks in PowerShell 7 and Windows
PowerShell 5.1, while target record, backup/baseline, binary, and runtime gates
remain open

Governance: GPL-3.0-or-later; OpenSSF-aligned target only, with no present
badge, certification, provider approval, or audit claim

## Active Working Object

All current work belongs under:

`_game_dev_ai_tracking/solutions/bit_wrecked_historical_random_start_0.0.5-alpha-tech-demo_2026-08-26/`

Human entry point:

`preview/START.bat`

The BAT currently opens a project-branded, player-facing WinForms preview. It
checks the game folder, presents exactly Standard and Random, and previews two
default-off, always-editable, mutually exclusive Random safeguard scopes:
`Starting biome only` and `All dangerous biomes`. It also
explains the planned nearest-valid-trader handling for the opening stone-shovel
quest after a Random start. It is not a gameplay mod, installer, world-policy
writer, or finished launcher. `Apply Preview Settings` records the selected
values only in the open preview session and confirms them in a compact popup
and the activity log. The popup has separate first-Apply, changed-pending, and
unchanged-refresh wording; it shows only the short state/change summary,
checked-folder signal, and preview-only boundary, without repeating the raw
game-folder path. The shell now distinguishes not-applied, pending, applied,
folder-check-required, invalid-folder, and canceled outcomes. A settings edit
becomes pending against the last confirmed snapshot; a path edit makes that
snapshot stale and disables Apply until the current folder is checked. None of
these states persists policy or changes the game.

## Current Approach

Borrow evidence from older work, not ownership.

Use a proof-first, two-lane product strategy. Prove the smallest authored-
spawnpoint relocation before building functional launcher policy machinery.
Keep arbitrary-coordinate Random search, drop behavior, protection, and
distance agency in a separately earned extension lane. These remain Random
features and may never create a third start type.

The completed Wasteland mod, neutral framework template, accepted module host,
and Offense Weapons work may be inspected to understand proven visual language,
logging, validation, accessibility, responsiveness, packaging discipline, and
failure handling. They are observation-only references and are not active
working directories for this project.

Do not modify those projects to build Historical Random Start - Alpha 6 Method. Do not copy
their gameplay payload, product-specific controls, histories, screenshots,
claims, or release identities into this solution.

## Current Assessment

- Project separation is strong.
- Visual continuity with the first Bit Wrecked mod is strong.
- The current shell is approximately a retargeted blank framework and remains
  disposable as architecture.
- The shell proves identity and visual lineage; it does not prove functional,
  accessibility, security, runtime, or release readiness.
- The newer accepted module host is the preferred infrastructure reference for
  DPI, High Contrast, accessibility, bounded logging, consent/path hardening,
  responsive validation, stale-result rejection, and capability gating.
- The completed Wasteland launcher is the behavioral and visual-history
  reference, not source to copy wholesale.
- The project manifest delegates reversible implementation judgment while
  keeping product semantics, writes, runtime authority, safety, and release
  decisions under explicit owner control.
- The player-facing preview now uses plain-language status, a `How It Works`
  popup, and a clear preview-only note. Runtime evidence, not visual
  completeness, controls promotion.
- Preview Apply edge coverage passes 11/11 in PowerShell 7 and Windows
  PowerShell 5.1. The production UI review confirms first Apply, Yes, No,
  pending edits, stale folder edits, reapply, activity entries, and pane
  expansion while the static scan still finds zero game/policy write paths.
- Random now previews one orderly row containing `Starting biome only` and
  `All dangerous biomes`. Both begin unchecked, remain editable under Standard
  and Random, and cannot both be selected. Starting-biome-only resolves the
  initial landing biome's reviewed hazard family while leaving every other
  dangerous biome active; all-dangerous-biomes resolves all four families.
  Standard ignores both, and they currently have no gameplay effect.
- Read-only V3.1 inspection identified distinct hazard/storm buff families for
  those four biomes plus one native whole-world `BiomeProgression` setting.
  That inventory bounds a future per-character suppressor; it does not prove
  that suppressor or authorize runtime work. The global setting may be read and
  respected but never silently changed by these controls.
- The core product should first prove new-character classification and one
  direct grounded relocation through the native authored spawnpoint list.
- Expanded Random complexity must not prevent a simpler proven old-style start
  from becoming useful.
- The current installed build reconfirms the server/client event distinction,
  world-native authored-point selector, native grounded/headroom validator, and
  a game-owned serializable CVar route for later exactly-once proof.
- The installed `Journey to Settlement` quest currently filters its first
  trader destination to Pine Forest and awards the stone shovel only through
  its native completion path. Static inspection found a promising native
  quest-accepted event and location setter, but their server authority,
  synchronization, persistence, and clean-client behavior require a separate
  Phase 1B proof after placement succeeds.
- Pure fixtures now prove the intended zero-action Standard path, lifecycle and
  duplicate rejection, one-attempt bounds, fail-closed marker/candidate states,
  and strict fictional target records. They do not prove game behavior.

## Design Direction

Preserve the recognizable outer Bit Wrecked language:

- brand, logo, project identity;
- short status card;
- visible game-folder context;
- one central task area;
- one truthful likely next action; and
- expandable Layered Reasoning Log / Recent Actions.

Replace inherited blank/Wasteland-specific controls with this project's actual
workflow:

- five recent save instances;
- browse-one and load-all paths;
- World, Status, and Spawn Policy information;
- ACTIVE/BYPASSED text with color reinforcement;
- exactly two policy controls: Standard (internal Vanilla) and Random;
- exact manually entered New Game name only; discovery and All Games hidden;
- trader continuity, biome safeguards, Random Distance, expanded coordinates,
  multiplayer, protection, drops, and rooftops disconnected until post-core;
- running-game read-only lock;
- explicit Apply confirmation and verified readback;
- Saved Settings Record; and
- always-visible direct-user-only `Launch Game`. Steam must be running; AI,
  timers, automation, and other controls cannot activate it.

Do not pursue pixel-perfect imitation. The implementing AI may improve native
controls, spacing, dimensions, reflow, internal architecture, and reversible
usability details when evidence supports the change and the reasoning is
recorded.

## Current Hard Boundaries

- Packaging/publication is additionally blocked until the complete GPL text is
  present and the pinned OpenSSF readiness controls have honest evidence.
- Provider acceptance, OpenSSF assessment, gameplay safety, and GPL compliance
  are separate gates; passing one does not imply another.

- Work only inside this current unpublished solution unless the owner explicitly
  expands scope.
- Older projects remain read-only observations.
- The owner-designated installed Steam root is eligible only as the private
  customer-grade Phase 1/Phase 1A lab. Builds/output remain externally staged;
  a separately approved exact payload may create only
  `Mods/mod_DEV/`.
- Existing saves/worlds, existing Mods and Harmony, core DLLs, game XML, and
  `_game_dev_ai_tracking` remain protected. The successful relocation target
  is exactly Navezgane / `HRS_Phase1A_Test_001`; the older no-`A` name is a
  different target and must not be substituted.
- Phase 1A placement has been proven once in the exact private lane. Do not
  infer authority for additional source amendments, recompilation, live DLL
  replacement, target reset, or another mutating run from that success.
- Lab designation is not wipe permission. Reinstall, broad deletion, or removal
  of any existing save/Mod requires separate approval naming the exact action.
- Do not implement biome-hazard suppression yet; the current checkboxes are a
  reviewed preview and future contract only.
- Do not implement starter-quest destination changes yet; Phase 1B remains a
  staged proof with a native-destination fallback.
- Do not enable policy writes, install, remove, or packaging before their
  contracts and gates are approved. Steam/game launch remains exclusively
  user-controlled even after the core bridge is ready.
- Do not add Developer Mode, console commands, cheat paths, telemetry, network,
  accounts, registry state, services, scheduled tasks, or hidden installers.
- Do not publish, push, upload, package, or distribute.
- Do not claim EAC, clean-client, multiplayer, accessibility, or release
  compatibility without direct evidence.
- Every later DLL or launcher/runtime bridge artifact must independently
  satisfy the source, pinned build, dependencies/SBOM, hashes, target
  allowlist, owned destination, scanner evidence, behavior disclosure, and
  rollback requirements in
  `n0151.md`.

## Current Open Decisions

No owner decision blocks bounded Alpha core implementation. The READY packet
resolves the bridge sidecar, Standard wording, customer-facing validation
wording, hidden deferred scopes, and omission of `Open Mods Folder`. Future
feature design remains deliberately unopened until the core completion gate.

Resolved: runtime does not require the game process to have been launched by
the Bit Wrecked GUI. It requires a valid saved policy, exact target match, and
all runtime gates. Manual use of the game's normal non-EAC launch option remains
valid.

Keep these decisions visible. Do not infer answers. Ask only one owner question
at a time when an answer becomes blocking.

## Current Next Action

1. Preserve the current successful Phase 1A artifact and exact completed
   Navezgane / `HRS_Phase1A_Test_001` target. Do not reset, recompile, or replace
   either merely to resume context.
2. Perform the already planned read-only persistence reload when the owner is
   ready. Acceptance requires the Completed marker to persist and no second
   selection or placement to occur. Record normal exit and sanitized evidence.
3. After persistence verification, decide whether to run a clean isolated
   replication before continuing Phase 1B. One success proves feasibility, not
   reliability.
4. Execute the bounded staged bridge implementation under
   `n0138.md`. Live deployment and game interaction
   remain exact human checkpoints.
5. Specify the first bridge as `ExactNewGameName` + Standard/Random + fixed
   new-player-first-arrival gate + per-player exactly-once marker. Keep
   `DiscoveredGame` mutation, `AllGames`, result-sidecar writes, and multiplayer
   disabled.
   Use the main plan's `Canonical Policy Eligibility Grid` and `PEG-*` row IDs
   as the single behavioral authority.
6. Do not connect the preview's biome safeguards or nearest-trader promise to
   runtime during the first bridge. Those remain Phase 1C and Phase 1B proofs.

Fresh-session reading order:

1. this file;
2. `n0009.md`, especially Target scope and
   Current Next Action;
3. `n0192.md`,
   especially sections 4.2, 4.3, 4.7, and 14;
4. `n0010.md` tail;
5. `evidence/n0043.md`;
6. `n0227.md`; and
7. `n0226.md`.

Do not reconstruct state from conversation memory. The exact-name distinction,
pending persistence reload, single-case limitation, and disabled future scopes
are controlling continuation facts.

## Review Rule

Use the previous work to ask, “What did this prove, and does that proof apply
here?” Never ask only, “What can we copy?”

The goal is a recognizable sibling product with better-fit world-policy UX and
the strongest proven infrastructure—not a renamed clone of the first mod.
