# Historical Random Start 0.0.5 Alpha Tech Demo — Start Here

Date: 2026-08-26
Status: QA CANDIDATE PACKAGED; CURRENT RANDOM/CENTRAL-SPAWN OBSERVATION PENDING QA
Player-facing product: `Historical Random Start - Alpha 6 Method`

Superseding QA update, 2026-08-27: read
`../../../HRS_DEV_QA_NEXUS_WORKFLOW.md` before acting. DEV remains this mutable
lane; `../../QA_Testing/` contains the immutable QA upload set; the peer
`0.0.1-alpha` lane remains frozen. The QA candidate contains the post-update
ItemValue compatibility repair and settling-aware verification correction.
The current QA question is whether exact-name Random is engaging or the player
is remaining at/appearing to receive a normal central authored/default start.
Do not classify that observation from appearance alone; QA must capture
eligibility, selected candidate, coordinates, lifecycle logs, marker, and hashes.

## Identity

This is a self-contained development lane inside the owner's intentional
hybrid live-game and development workspace. The folder boundary communicates
authority; it is not a Git branch boundary.

Active lane:

`bit_wrecked_historical_random_start_0.0.5-alpha-tech-demo_2026-08-26`

Frozen reference:

`bit_wrecked_historical_random_start_0.0.1-alpha_nexus-review_2026-08-26`

Current version mapping:

- development/release identity: `0.0.5-alpha-tech-demo`;
- game-compatible `ModInfo.xml` version: `0.0.5`;
- policy, result, deployment, history, and recovery schemas: remain `v1`;
- older proof probes and dated contracts: remain historical `0.0.1` records;
- visual-only preview: remains the historical `0.0.7-preview` baseline.

## Authority

Routine source, documentation, tests, and contained build outputs may change
only inside this active lane. The frozen lane and Nexus repository are
read/hash-only.

Live `Mods`, `Data/Config`, saves, worlds, Steam files, executables, managed
assemblies, Harmony, and anti-cheat state remain external. Any mutation or game
launch requires a separate owner instruction naming the exact action.

No commit, push, package, tag, upload, or publication is implied by local work.

## Current Implementation Truth

The active lane contains:

- the established WinForms manager entry and launcher modules;
- strict exact-game-name Standard/Random policy contracts;
- bounded deployment inventory and removal contracts;
- local history and recovery-state contracts;
- one release runtime entry,
  `HistoricalRandomStartModApi.InitMod`, using the native
  `PlayerSpawnedInWorld` event;
- fail-closed environment, lifecycle, policy, marker, candidate, placement,
  verification, and result gates; and
- extensive pure, static, deployment, management, and preview tests.

The required non-live suites pass 271/271 checks in PowerShell 7 and 271/271
in Windows PowerShell 5.1. See
`n0005.md` for the legacy long-path host
caveat and exact claim boundary.

Older retained `out` and `tmp` contents predate the
current corrections and remain historical generated evidence. A current
deterministic QA candidate exists only through the hash-identified package
routed by `../../QA_Testing/README.md`. It is not approved as a Nexus/public
payload, and the QA laptop must validate its game build before staging.

This lane is unpublished and is not Nexus-approved, public-release-ready, or
proven compatible with EAC, multiplayer, dedicated servers, or clean clients.

## Existing Main Pipeline

Do not add another main function, launcher, orchestrator, state authority, or
deployment path.

```text
existing BAT / direct-user manager action
  -> existing WinForms manager script
  -> launcher validation and capability modules
  -> deployment/policy/history/recovery contracts
  -> direct-user-only game-launch boundary
  -> HistoricalRandomStartModApi.InitMod
  -> environment and lifecycle gates
  -> Standard bypass or bounded Random relocation
  -> sanitized result and local history projection
```

New work must attach as a bounded feature through the extension points mapped
in `n0003.md`.

## Generated Output

`out/`, `tmp/`, `*.dll`, `*.pdb`, and manager runtime
state are ignored generated/local material. Do not edit them as source. Retain
existing artifacts as evidence until a separately scoped rebuild or evidence
retirement decision.

## Private and Security Material

`security/j0207.json` contains machine-specific paths
and private lab targeting information. It is development-only and forbidden
from release packages, Nexus copies, and public publishing.

No development key belongs in this lane. A key present in an external review
folder remains external and must not be read, copied, logged, or packaged.

Read `n0006.md` before any copy or
promotion work.

## Non-Live Validation

Run the following from this active lane in their supported PowerShell hosts:

- `tests/p0168.ps1`;
- `tests/p0169.ps1`;
- `tests/p0170.ps1`;
- `tests/p0171.ps1`;
- `tests/p0172.ps1`;
- `tests/p0173.ps1`;
- `tests/p0174.ps1`;
- `tests/p0175.ps1`;
- `tests/p0177.ps1`;
- `tests/p0178.ps1`; and
- `src/runtime/main/p0228.ps1`.

These tests must not launch Steam or the game or write outside the active lane.

## Known Blockers and Deferred Features

- No current `0.0.5` artifact has passed clean-laptop QA or been promoted to a
  new Nexus-review candidate.
- The apparent normal/default central-map Random start is the current QA
  observation. Eligibility, authored candidate, and final position remain to be
  distinguished with logs and coordinates.
- QA staging and runtime observation require new owner checkpoints on that
  machine; QA does not repair or rebuild locally.
- Persistence/reload and broader runtime reliability evidence remain bounded
  human/live-game work.
- Trader routing, biome safeguards, discovery, All Games, multiplayer,
  expanded-coordinate placement, protection, drops, rooftops, and other
  post-core features remain disconnected unless separately opened.
- GPL, OpenSSF, packaging, provider-review, and public-release gates remain
  separate and must not be inferred from passing non-live tests.

## Next Approved Bounded Task

Run the immutable candidate through the clean-machine QA route in
`../../../HRS_DEV_QA_NEXUS_WORKFLOW.md`. Return any defect evidence to DEV. Do not
open a new gameplay feature merely because an extension point exists; core
Random activation/location behavior and persistence/no-repeat come first.

## Read Next

1. `n0132.md`;
2. `n0152.md`;
3. `n0150.md`;
4. `n0007.md`;
5. `n0003.md`;
6. `src/runtime/n0212.md`;
7. `n0006.md`; and
8. only the historical contracts/evidence routed for the task.
