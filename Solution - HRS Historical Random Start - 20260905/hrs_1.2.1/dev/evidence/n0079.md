# Phase 1A Marker-Only Source Scaffold Gate

Date: 2026-08-20
Decision: Source scaffold/static-test GO; compile/install/run NO-GO

The owner authorized Phase 1A source scaffolding. A separate marker-only DEV
source tree was created for the future disposable target
`Navezgane / HRS_Phase1A_Test_001`. No existing Phase 1 observation source was
modified.

The scaffold contains five C# files and `ModInfo.xml`. Its only prospective
game-state mutation is one network-synchronized write of Reserved value `1` to
the namespaced player CVar `bitwrecked_hrs_state_v1`, after all build, target,
EAC, authority, single-player, local-player, lifecycle, entity, and absent-marker
guards pass. The write is immediately read back. There is no Completed write.

Static source checks passed under PowerShell 7 and Windows PowerShell 5.1:

```text
PHASE1A_MARKER_STATIC_SUMMARY pass=1 fail=0 csFiles=5 relocationCalls=0
```

The 16-case Phase 1A pure state-machine suite also remained 16/16 in both
shells. The installed assembly was read-only inspected and confirms
`SetCustomVarNetwork(String, Single, CVarOperation)`, `HasCustomVar(String)`,
`GetCustomVar(String)`, and enum member `CVarOperation.set`.

Pinned source aggregate SHA-256:
`A60E1AA92E929F22F6BA446584485E042C056C1FD14B37DA932B70EBE7575085`

No compiler was invoked. No DLL, external build output, Mod folder, Phase 1A
save, backup, or runtime log exists. Compilation requires a new explicit owner
authorization and must produce an external-only marker probe for static review;
installation and execution remain later independent gates.
