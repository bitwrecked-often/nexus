# Phase 1A Monotonic Settling and Categorical Verification — Source Amendment

Date: 2026-08-20
Decision: Source/static/pure tests PASS; compile/stage/live/run NO-GO

The owner authorized the verification amendment proven by read-only analysis.

The probe still waits for at least two `GameUpdate` callbacks, but terminal
verification is now bounded by a 30-second Unity
`Time.realtimeSinceStartup` deadline rather than 120 update callbacks.

All original acceptance conditions remain required. If they do not all pass by
the deadline, exactly one fixed categorical reason is emitted:

- `RELOC_VERIFY_CONTEXT_FAILED`;
- `RELOC_VERIFY_CHUNK_TIMEOUT`;
- `RELOC_VERIFY_UNSAFE`; or
- `RELOC_VERIFY_POSITION_MISMATCH`.

No coordinate, distance, identity, hash, state value, or candidate value is
logged. The generic `RELOC_VERIFICATION_FAILED` reason and
`LastVerificationTick` are statically forbidden.

The amendment adds no retry, reroll, second placement, explicit chunk request,
rollback, teleport, respawn, or marker repair/removal. Semantic-after capture
and Completed remain reachable only after context, bounds, chunk readiness,
safety, and position tolerance all pass.

Relocation static tests, semantic harness tests (7/7), and all Phase 1A contract
tests (22/22) pass under PowerShell 7 and Windows PowerShell 5.1.

Updated source aggregate SHA-256:

`DC63CBAF21EC0A3A01AD549DCA82CEB2610B7C795B019F3AD63AF232C0E602AD`

No compiler ran. The live DLL remains the superseded
`1F68045943DACF40DA579D3F62C3D5BD48F2D20F3F31521203F04D93EADF5F54`
artifact, and the failed Reserved target remains present. Neither may be reused.
