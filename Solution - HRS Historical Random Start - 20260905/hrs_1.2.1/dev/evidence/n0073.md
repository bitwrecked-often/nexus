# Phase 1A Marker Probe — First-Load Metadata Rejection

Date: 2026-08-20
Decision: Runtime attempt invalid; probe not loaded; marker proof NOT RUN

The owner launched the exact disposable `Navezgane / HRS_Phase1A_Test_001`
new game after the operational preflight passed. The game created and loaded
the target normally, but the mod loader ignored the owned DEV folder before
loading the assembly.

The dedicated log contains exactly one invalid-Name error and one subsequent
ModInfo parse/ignore error. `ModInfo.xml` used the internal value
`BitWrecked Historical Random Start Phase 1A Marker Probe`; the installed game
requires the internal `Name` to match `^[0-9a-zA-Z_\-]+$`.

There are zero `[HRS-P1A-MARKER]` lines, including zero `MARKER_READY` and zero
reservation result lines. The probe assembly never loaded, so it could not
write `bitwrecked_hrs_state_v1`. This is a metadata validation failure, not a
marker API/runtime result.

The user exited normally. Post-exit checks confirm the game process is closed,
the target save exists, and the two live owned files retain their reviewed
hashes. No live correction, save deletion, restart, or retest has been
performed.

Recovery requires two separately reviewed actions: correct the internal
metadata Name to an allowlisted token and restore the exact disposable target
to its pretest absent state (after preserving evidence). Neither action is
authorized by the completed launch gate.
