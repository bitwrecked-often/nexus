# Historical Random Start 1.2.0 payload location

The distribution payload is stored under `dev/verified/main`. The manager pins
both release-file hashes and refuses drift before deployment.

This folder remains documentation-only so there is one authoritative packaged
runtime and no ambiguous duplicate DLL.
