# Phase 1A AddCustomVar Probe — First-Load Success

Date: 2026-08-20
Decision: Immediate reservation PASS; persistence pending; relocation NO-GO

The exact AddCustomVar live payload passed preflight and loaded on the new
disposable `Navezgane / HRS_Phase1A_Test_001` target. Sanitized output contains
exactly:

```text
[HRS-P1A-MARKER] v=0.0.1 build=b14 reason=MARKER_READY
[HRS-P1A-MARKER] v=0.0.1 build=b14 reason=MARKER_RESERVED_VERIFIED
```

There are zero failure, mismatch, rejection, absent, invalid, or internal
results. This proves `AddCustomVar(Name, 1f)` made the Reserved marker
immediately observable through the same guarded server callback. No retry or
second write occurred.

The user exited normally. The game is closed. Both live payload hashes remain
exact. The target contains 68 files with post-first-load aggregate SHA-256:

`F58682035F814683694B63449738B35DC0707FA15E850D53B56EDB40CB072946`

Persistence across normal save/reload is not yet claimed. One separately
authorized read-only `LoadedGame` observation must report
`MARKER_RELOAD_RESERVED`. That branch cannot write the marker. Relocation
remains NO-GO until persistence evidence passes and the marker-only test is
reviewed.
