# Phase 1A Containing-Chunk Relocation — First-Load Result

Date: 2026-08-20
Decision: Containing-chunk readiness acceptance FAIL/NO-GO

The exact local single-player Navezgane target was created and the player
spawned without intentional interaction. Sanitized runtime evidence contains:

```text
[HRS-P1A-RELOC] v=0.0.1 build=b14 reason=RELOC_READY
[HRS-P1A-RELOC] v=0.0.1 build=b14 reason=RELOC_PLACEMENT_CALLED
[HRS-P1A-RELOC] v=0.0.1 build=b14 reason=RELOC_VERIFY_CHUNK_TIMEOUT
```

The verifier used only read-only containing-chunk lookup and did not call the
broader area-loaded predicate. The candidate's containing chunk did not become
available during the 30-second monotonic window. Native safety, position
acceptance, semantic-after comparison, and Completed were not reached. The
marker remains Reserved and this target must not be retried.

Post-close aggregate evidence is stored at:

`C:\BitWreckedDisposable\HRS_Phase1A\BuildStage\phase1a-containing-chunk-post-timeout-2026-08-20.json`

All protected aggregates exactly match the containing-chunk baseline. The
isolated target has 74 files, 19,295,385 bytes, and aggregate
`13D6773CC3DB13F35B0AEDFCB769685D2767784758DA5CF9B375D8A1C97FF909`.
The installed DLL remains
`91C0AE24599088C64485C29851847CD5C703CF4C0ADF7A1D72C3D9C371417CCD`.

This falsifies the prior hypothesis that only neighboring chunks prevented
acceptance. It does not yet distinguish scheduler non-consumption from native
post-callback player/controller repositioning that overwrote the candidate and
then the observer desired position. No further mutation is authorized.

Next gate: authorize Phase 1A post-placement player/observer drift analysis.
