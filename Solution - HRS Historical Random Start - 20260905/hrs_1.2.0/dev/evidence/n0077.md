# Phase 1A Marker-Only Probe — External Payload Staging Record

Date: 2026-08-20
Decision: External two-file payload PASS; installation/execution NO-GO

The owner authorized staging the reviewed Phase 1A marker-only probe as an
external payload. A new directory was created at:

`C:\BitWreckedDisposable\HRS_Phase1A\BuildStage\payload-0.0.1-dev`

It is outside the game installation and save storage, is not a reparse point,
and contains exactly:

| File | Bytes | SHA-256 |
| --- | ---: | --- |
| `marker.dll` | 8,192 | `FCC012FC8841FC1D7285C108D15EDDC4DD62BED5500B0C3F387F918AFE4F37E3` |
| `ModInfo.xml` | 407 | `439777B85A3E9288F9DC2F00B5A9DD0527D29CD48EBC3DAACFEA037A540CD6F2` |

The SHA-256 of the two ordinal `path|bytes|sha256` UTF-8 lines joined by LF is:

`D041F98190EAA46918B0C1C9FD64A8D875EF148E3B02A65C82FA267AA38BF744`

The DLL exactly matches both reviewed deterministic builds. `ModInfo.xml`
matches the pinned source and retains `SkipWithAntiCheat=true`. Microsoft
Defender scanned the complete payload and reported no threats (exit `0`).

No payload manifest or other evidence file was placed inside the candidate
payload. No live `Mods` folder or save was created, and the game was not
launched. Copying these exact two files into one exact owned DEV Mod folder
requires separate owner authorization; execution remains a later gate.
