# Phase 1A Native-Aligned Relocation — First-Load Result

Date: 2026-08-20
Decision: Safe guarded abort PASS; relocation acceptance FAIL/NO-GO

The owner authorized the native-aligned first-load launch. The exact reviewed
probe loaded and initialized, and the approved local single-player Navezgane
target `HRS_Phase1A_Test_001` was created. The player reported spawning and
exited normally without intentional changes.

Sanitized runtime evidence contains exactly:

```text
[HRS-P1A-RELOC] v=0.0.1 build=b14 reason=RELOC_READY
[HRS-P1A-RELOC] v=0.0.1 build=b14 reason=RELOC_PLACEMENT_FAILED
```

`RELOC_PLACEMENT_CALLED` is absent. Therefore the guarded block failed after
reservation/selection validation but before placement was confirmed and before
pending verification was registered. No semantic-equality or Completed result
exists. The marker remains Reserved and this target must not be retried.

Post-close aggregate-only evidence was captured at:

`C:\BitWreckedDisposable\HRS_Phase1A\BuildStage\phase1a-native-aligned-post-placement-failed-2026-08-20.json`

All protected aggregates equal the pre-launch baseline exactly:

| Protected tree | Result | SHA-256 |
| --- | --- | --- |
| Game managed | unchanged | `2EA9679D59A7DC759DC4F582A6673B0F2106CE475DA4F7FF998DE907BFCD5C4C` |
| Foreign Mods | unchanged | `B56FE9701B98231617DBED2431865CA077AF4B84105C26388496A336D056BD51` |
| Foreign saves | unchanged | `CE3564E1E1BB04F8348A1570D36CA8302641ABCA2EFE509A495C5366FAC1C23D` |

The failed exact target contains 74 files and has aggregate
`CFFC1EBA37258EBD8B34C7D3BD284DB1C6263943FC84606DDF37CEA3C5EB20E1`.
No identities, coordinates, filenames, or underlying semantic values were
published.

The broad guarded block includes world-GUID acquisition, semantic-before
capture, virtual `SetPosition`, pending-state creation, and the placement-called
log. Read-only failure isolation is required before any source amendment.
