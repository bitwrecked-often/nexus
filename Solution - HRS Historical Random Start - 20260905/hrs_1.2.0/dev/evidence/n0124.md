# Phase 1A Semantic Digest Unchecked Arithmetic — Source Correction

Date: 2026-08-20
Decision: Source/static/pure tests PASS; compile/stage/live/run NO-GO

The owner authorized the minimal source correction proven by placement-failure
analysis. Explicit `unchecked` scopes now surround the complete string and
float FNV digest loops. The integer digest path retains its existing explicit
`unchecked` scope.

The static test now requires exactly three explicit unchecked blocks in
`c0218.cs`. Compiled-artifact review remains responsible for proving
ordinary wrapping `mul` opcodes and zero `mul.ovf`/`mul.ovf.un` opcodes in all
digest overloads.

No semantic field, readiness ordering, random selection, placement, marker,
logging, or verification behavior changed.

Relocation static tests, semantic harness tests (7/7), and all Phase 1A contract
tests (22/22) pass under PowerShell 7 and Windows PowerShell 5.1.

Updated eight-file source aggregate SHA-256:

`506C47B830026A777D230AF42E647E16B9994D7921E9DACEC4061C03EEABABD4`

No compiler ran. The live DLL remains the superseded
`91665756F96640D08F31F744B3C12E09236A189BD91EF4F4E972D46416AB6290`
artifact, and the failed Reserved target remains present. Neither may be reused.
Recompilation and all later mutations remain separately gated.
