# Phase 1 First Load — Steam Prerequisite Attempt

Date: 2026-08-20
Decision: Retry required after user-authenticated Steam session

The authorized EAC-disabled client started with the exact approved payload.
The probe loaded and emitted one sanitized initialization line:

```text
[HRS-P1] v=0.0.1 build=b14 authority=pending locality=pending lifecycle=init entity=false count=0 elapsedMs=0 reason=OBS_READY
```

No HRS load error was detected. The client then exited because Steam was not
running/authenticated. The target save remains absent, so no gameplay/runtime
case began and no rollback is required.

The privacy-safe extractor was corrected to remove the ordinary game-log
timestamp prefix before validating the allowlisted HRS portion. This changes
test tooling only; the already-built and installed DLL is unchanged.
