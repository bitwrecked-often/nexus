# Phase 1A Atomic-Semantic Relocation — External Build Record

Date: 2026-08-20
Decision: Deterministic build and compiled-IL review PASS; staging/live/run NO-GO

The owner authorized recompilation of source aggregate
`CDCDFA4022DB15FE5B50E2251E681F84C34E51962DBE3C27E73FDDBDD023CDEF`.
The pinned `dotnet.exe`, Roslyn `csc.dll`, and all seven explicit game/runtime
references matched their recorded SHA-256 fingerprints. The game and server
were closed.

| Build | Exit | Compiler output | Files | Bytes | SHA-256 |
| --- | ---: | ---: | ---: | ---: | --- |
| `build-25-relocation-atomic-semantic` | 0 | 0 lines | 1 | 15,360 | `632208686E0C5A2156DC27B32FD3579F80A0AC9E60171B262E1147AEEE8EFCE2` |
| `build-26-relocation-atomic-semantic` | 0 | 0 lines | 1 | 15,360 | `632208686E0C5A2156DC27B32FD3579F80A0AC9E60171B262E1147AEEE8EFCE2` |

The two clean external outputs are byte-identical. Assembly name is
`reloc`, version `0.0.0.0`,
and MVID `0bda2181-e3e1-4d36-84a4-350351f19040`. Each build directory contains
only the expected DLL.

Mono.Cecil method-level inspection confirmed for both artifacts:

- `OnPlayerSpawned` has zero placement, observer-position, and semantic-capture
  calls;
- `OnGameUpdate` has two semantic captures, one player placement, one
  existing-observer synchronization, and one `BeginPlacement(float)` call;
- one native random selection, one containing-chunk lookup through
  `WorldBase.GetChunkFromWorldPos`, one native spawn-safety call, and two marker
  `AddCustomVar` calls;
- zero teleport, respawn, direct network CVar mutation, explicit chunk request,
  and observer add/remove calls.

Microsoft Defender custom-scanned both exact DLLs with exit code `0`.

No payload was staged or installed. The live DLL remains
`3BB52A412C6B156D849FEC1D2843BF9CD32A0ED0F44BC0CB39E152E2CD37D611`.
The exact Reserved disposable target remains present with 68 files and
19,377,481 bytes. Nothing was launched or reset.

Next gate: **authorize atomic-semantic Phase 1A relocation payload staging**.
