# Phase 1A Containing-Chunk Readiness Verification — Source Amendment

Date: 2026-08-20
Decision: Source/static contract PASS; compilation/live/save/run NO-GO

The owner authorized only the containing-chunk readiness verification source
amendment.

## Amendment

- Removed `World.IsChunkAreaLoaded(candidate)` from the relocation source and
  prohibited it in the static contract.
- Resolve the candidate's containing chunk read-only with
  `GetChunkFromWorldPos(World.worldToBlockPos(candidate))`.
- While that containing chunk is absent, wait only within the existing
  30-second monotonic deadline; classify expiration as
  `RELOC_VERIFY_CHUNK_TIMEOUT`.
- Once the containing chunk exists, call native
  `CanPlayersSpawnAtPos(candidate, false)` exactly once. False is immediately
  and categorically `RELOC_VERIFY_UNSAFE`.
- Perform the position-tolerance decision once after safety; mismatch is
  immediately `RELOC_VERIFY_POSITION_MISMATCH`.
- Preserve the single selection, player placement, existing-observer
  synchronization, semantic comparison, and completion sequence.

No explicit chunk request, observer lifecycle operation, retry, reroll, second
placement, rollback, teleport, respawn, or marker repair was added.

## Source identity

- Status: `ContainingChunkReadinessVerifierAmendedUncompiled`
- Source aggregate SHA-256:
  `61DADC773FCF21020B6E7DAA4DBE21B3828DE0EA17A2EEFAE17A7739E935B714`
- `c0211.cs`: 6,414 bytes,
  `29B68B7D5DB474DBC9B1C1896560DA743615BF358704D8736C2AF7B3433C6CA6`

## Verification

Both `pwsh.exe` and Windows `powershell.exe` passed:

- relocation static checks: 1 pass, 0 failures;
- semantic harness: 7 passes, 0 failures; and
- pure contract suite: 23 passes, 0 failures.

The game remained closed. The live DLL remains unchanged at
`9FBF9CDEBAB108633E916D338F6D9571D282533A4E6E3FBC51AB529EB1825FAB`.
The failed Reserved target remains present and was not changed. No compilation,
staging, installation, reset, or execution occurred.

Next gate: authorize containing-chunk Phase 1A relocation recompilation.
