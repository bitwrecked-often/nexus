# Phase 1A Relocation Probe — External Build Record

Date: 2026-08-20  
Decision: External deterministic build/static review PASS; stage/live/run NO-GO

The owner authorized recompilation of exact relocation source aggregate
`861F5699B465351D857559EC35BA03BCE2B1897E9924F8E4861ED07C7CA5A53D`.
Two new clean external directories were used. The pinned compiler and all seven
explicit game/runtime reference hashes matched before compilation.

| Build | Exit | Output | Files | Bytes | SHA-256 |
| --- | ---: | ---: | ---: | ---: | --- |
| `build-07-relocation` | 0 | 0 lines | 1 DLL | 10,752 | `14E101DCA80148D7589E6B7EFE16532F4C22AB94355EA2605469B53A2784378E` |
| `build-08-relocation` | 0 | 0 lines | 1 DLL | 10,752 | `14E101DCA80148D7589E6B7EFE16532F4C22AB94355EA2605469B53A2784378E` |

The outputs are byte-identical. Assembly name is
`reloc`, version `0.0.0.0`,
MVID `977c0784-7e6e-4623-b1a9-0d8c9689b7ca`.

Cecil inspection found 139 method calls, 69 distinct, with exact reviewed
counts:

- `EntityBuffs.AddCustomVar`: 2 (Reserved and Completed transitions);
- `SpawnPointList.GetRandomSpawnPosition`: 1;
- `Entity.SetPosition`: 1;
- `World.CanPlayersSpawnAtPos`: 2 (candidate and verification);
- `Vector3.Distance`: 1; and
- direct `EntityBuffs.SetCustomVarNetwork`: 0.

There are zero teleport, respawn, marker-removal, alternate random-selection,
file-write/delete, process-launch, or Harmony calls. Required ready, placement,
verification-failure, and completion reason strings are compiled in.

Microsoft Defender scanned both DLLs and reported no threats with exit `0`.
No relocation live folder exists, the game remained closed, and the existing
marker-proof save remained present and unchanged by compilation.

External payload staging is a separate gate. Before live installation or
execution, the existing marker probe and persistent-Reserved test save require
explicit handling, and the semantic state-delta harness must be pinned.
