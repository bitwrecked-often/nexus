# Phase 1A atomic placement-semantic comparison source amendment

Authorized scope: source amendment only.

## Amendment

- Retained the comprehensive semantic digest.
- Captures the same full semantic snapshot immediately before placement and
  immediately after player placement plus existing-observer synchronization,
  within the same deferred `GameUpdate` callback.
- Treats an atomic mismatch as terminal before starting verification.
- Logs semantic equality before `BeginPlacement` establishes the later-tick
  verification deadline.
- Removed the stored pre-placement snapshot and all later-tick semantic
  recapture/comparison, preventing ordinary adjacent-update drift from being
  attributed to placement.
- Updated the pure contract with the atomic semantic gate and its categorical
  failure case.
- Updated the static source contract and prebuild packet to enforce this order.

## Verification

Both `pwsh.exe` and `powershell.exe` passed:

- relocation static source test: 1 pass, 0 failures;
- semantic harness: 7 passes, 0 failures;
- pure contract: 25 passes, 0 failures.

The static contract requires exactly two `SemanticSnapshot.Capture` calls and
orders them around the single player placement and single existing-observer
synchronization before verification begins.

## Source identity

- Aggregate method: SHA256 of UTF-8 ordinal `path|bytes|sha256` lines joined by
  LF; `j0219.json` excluded.
- Aggregate SHA256:
  `CDCDFA4022DB15FE5B50E2251E681F84C34E51962DBE3C27E73FDDBDD023CDEF`
- `c0185.cs`: 866 bytes,
  `EB84464DA8F59AE11023F0A47A8D2B087F0BD1FEF5735200157812B5722D594B`
- `c0211.cs`: 7306 bytes,
  `BA5B23ACA86F50F9569998C97EC71A61F3D74E449C05531FE1D57DE51EDFF3B9`

## Boundary confirmation

- No compilation.
- No payload staging or installation.
- No live DLL replacement.
- No save backup, reset, or mutation.
- No game launch.
- Live DLL remained
  `3BB52A412C6B156D849FEC1D2843BF9CD32A0ED0F44BC0CB39E152E2CD37D611`.
- Exact disposable target remained present at
  `%APPDATA%\7DaysToDie\Saves\Navezgane\HRS_Phase1A_Test_001`.

Next gated action: **authorize atomic-semantic Phase 1A relocation recompilation**.
