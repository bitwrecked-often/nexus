# Phase 0A Pure Contract Test Report

Date: 2026-08-14  
Scope: Pure state/decision and private-target record fixtures  
Result: 56 passed, 0 failed in each of two PowerShell hosts

## Boundary

These tests exercised project-owned pure models and fictional JSON fixtures.
They did not load a game assembly into a game process, start 7 Days to Die,
compile a DLL, create a staged game copy, inspect a real save, or modify any
game, `Mods`, Harmony, XML, world, save, registry, service, task, or network
state.

The game and dedicated-server processes were not running during either test
run.

## Test hosts and commands

| Host | Version | Result |
| --- | --- | --- |
| PowerShell | `7.6.4` | `SUMMARY pass=56 fail=0` |
| Windows PowerShell | `5.1.26100.9168` | `SUMMARY pass=56 fail=0` |

Commands were run without an execution-policy bypass:

```powershell
& '.\tests\p0171.ps1'
& powershell.exe -NoProfile -File '.\tests\p0171.ps1'
```

## Case composition

| Group | Count | What it proves in the pure model |
| --- | ---: | --- |
| JSON fixture parse | 1 | Target schema JSON is syntactically readable |
| Phase 1 observation decisions | 9 | Build, target, EAC, authority, lifecycle, entity, and duplicate gates leave vanilla behavior untouched |
| Phase 1A/core decisions | 33 | Standard zero-action, one-attempt bounds, lifecycle rejection, marker states, native-list failures, candidate failures, placement/completion failure states, and success-state transitions |
| Target-record decisions | 13 | Exact strict shape, outside-live-root rule, UNC/traversal rejection, no identity field, no EAC, first-lane execution, fingerprint match, separate backup, and UTC timestamps |

Every decision case also asserts:

- no more than one reservation write;
- no more than one native selection;
- no more than one placement attempt;
- no more than one completion write; and
- zero modeled shell, network, registry, service, task, XML, or file side
  effects.

## Static operation scan

The PowerShell test/model source was scanned for process launch, expression
evaluation, web/network clients, registry mutation, destructive file commands,
and common file-writing commands. Result:

`STATIC_FORBIDDEN_OPERATION_SCAN=NO_MATCHES`

All four project JSON files parsed successfully after the test run.

## Exact tested-file hashes

| File | Size (bytes) | SHA-256 |
| --- | ---: | --- |
| `tests/m0198.psm1` | 16,788 | `21533609ADF7EB4DCC326FBA9F14C6AA6D499A5927D79EE7DFB5AFF1A24CC081` |
| `tests/p0171.ps1` | 6,418 | `F8F6A9498D0F5AD59C9DD12FF4478D7008CF95FA2C697A3A5EDDAA2937271152` |
| `tests/fixtures/j0197.json` | 11,767 | `D2C8EE62A86EF187F11113498FB85447D3F6DCFEDBE266D158D0FAF050E6C080` |
| `tests/fixtures/j0201.json` | 3,452 | `F2F468F32A4398D7581E5D0DEA9DF1B1B22A31CE161E28189EDD9F099ACFE6EB` |
| `tests/fixtures/j0202.json` | 1,135 | `2668CE02AF6001F84B0FF061FBEBF05F505CCB5726343689CB65C548E5B7D6B9` |
| `src/runtime/j0199.json` | 5,664 | `680AFC0B5A8EFE6EDB396E703AED19AF36BDE970D519A09BE3AA3B6601BFDFCE` |

## Negative-matrix interpretation

The pure run supports the model layer of P0A-019 and P0A-024 through P0A-036,
plus the bounded-counter portion of P0A-046 and no-EAC fail-closed portion of
P0A-048. It does not turn any required runtime observation into a pass.

The two-choice revision keeps the suite at 56 cases. `CORE-004` now proves that
a stale removed third-mode value returns `POLICY_MODE_INVALID` with zero native
selection and zero placement attempts.

In particular, reparse-point behavior, real event ordering/count, actual save
survival, crash durability, authoritative placement, progression neutrality,
EAC loading, clean-client behavior, listen/dedicated behavior, and rollback all
remain pending.

## Conclusion

Pure-model gate: PASS.  
First DLL gate: NO-GO.  
Runtime/core behavior gate: NO-GO pending an approved disposable target,
backup, source review, compile authorization, and direct evidence.
