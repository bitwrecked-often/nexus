# Historical Random Start 1.0.1 release manifest

Release date: 2026-08-28  
Solution lane: `solutions/hrs_1.0.1`  
Game target: 7 Days to Die V3.2 (b9), Navezgane, local single-player  
Anti-cheat: disabled (`SkipWithAntiCheat=true`)

## Player features

- Portable PowerShell 5.1 manager launched through root `START.bat`.
- Exact-name Standard mode that leaves starting behavior unchanged.
- One-shot Random mode using twelve curated Navezgane survey anchors.
- Deferred placement and safe-landing verification after world chunks load.
- Optional suppression of the initial NG01 snow hazard only for the relocated
  character and selected game.
- Nearest opening-trader routing across Navezgane biomes.
- Vanilla quest lifecycle, rewards, trader interaction, and later progression
  remain intact.

## Trader implementation

V3.2 stores the starter quest on `EntityPlayerLocal`, normalizes its runtime ID
casing, and exposes some trader registries only transiently or server-side. The
release therefore:

1. observes the local player's journal for the full new-game session;
2. matches only `quest_whiteRiverCitizen1`, phase-one objective `trader`;
3. uses the five version-pinned trader placements and bounds authored in the
   installed Navezgane `prefabs.xml` and trader prefab XML files;
4. chooses the nearest placement deterministically by planar distance;
5. updates only that objective through native `ObjectiveGoto.SetLocation`;
6. records one-time Pending, Reserved, and Completed state on the character.

No base-game file is edited. Standard mode, unrelated saves, other quests, and
other worlds receive no trader mutation.

## 1.0.1 packaging identification correction

QA found that the DLL loaded by the application did not match the DLL version
identified in the package notes, even though the package contained and loaded
the correct DLL binary. The package's identifying version information and
release notes were corrected in `1.0.1` to match the verified DLL actually
included and loaded.

This was a packaging-identification correction made to keep QA, deployment
verification, and troubleshooting aligned with the delivered artifact. The DLL
functionality and application behavior were not changed by this correction.

## Live QA proof

`HRS_TRADER_006` completed the full fresh-game path without reload:

`NG01 -> RELOCATION_COMPLETED -> TRADER_ROUTE_RESERVED -> TRADER_ROUTE_SELECTED -> TRADER_ROUTE_COMPLETED`

The Journey to Settlement destination changed from the Pine Forest trader at
about 2.4 km to the closest authored trader. No fallback or exception appeared.

## Release verification

The final release must be compiled after the game closes. Record the final DLL
SHA-256, deterministic double-build result, package inventory, and gate totals
below before distribution.

- Runtime SHA-256: `F9B937E05E914B52DEA9E379EB4EA8C0CAB716764F87E543FA44E5456477CCC6`
- ModInfo SHA-256: `E32B4F6D7AE07BA1272EB88ABE7FBDF402E1E6F6B2DA3674B71314DF24E1E94B`
- Runtime size: 38,400 bytes
- Deterministic double build: passed
- Core contract: 89 passed, 0 failed
- State/deployment: 62 passed, 0 failed
- Relocation static: passed
- Portable launcher: 23 passed, 0 failed
- Release-source static: passed

The full `1.0.1` packaging incident, automated gate results, user-observed
Apply outcome, limitations, and DEV next steps are recorded in `n0235.md`.

The authoritative distribution payload is `dev/verified/main`. Generated build
and scratch directories were moved outside the release tree after verification.
The release tree contains one DLL and no path component of 12 or more
characters.
