# Historical Random Start - Alpha 6 Method Payload Placeholder

This directory intentionally contains no `ModInfo.xml`, DLL, XML patch, runtime
helper, installer, or package payload.

When implementation is explicitly authorized, the first runtime work belongs
in a staged source/version lane—not in the live game `Mods` folder. Every file,
identifier, dependency, write boundary, and removal behavior must come from the
approved project manifest, Phase 0A contract, and test evidence.

Do not copy Wasteland animal XML or another mod's gameplay payload here.
