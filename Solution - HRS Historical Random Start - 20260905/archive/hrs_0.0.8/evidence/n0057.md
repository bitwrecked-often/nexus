# Phase 1A Corrected Semantic Relocation — External Build Record

Date: 2026-08-20  
Decision: Deterministic external build/static review PASS; stage/live/run NO-GO

The owner authorized recompilation of corrected source aggregate
`7E4F06FB967E6867006B0629C27180ECF97C6B9731569608A0C4BD9EDC20A6A1`.
The pinned compiler and all seven explicit read-only game/runtime references
matched their recorded SHA-256 values. The game and server were closed.

The prior failed `build-09-relocation-semantic` directory was left untouched
and empty. Two fresh external build directories were used:

| Build | Exit | Files | Bytes | SHA-256 |
| --- | ---: | ---: | ---: | --- |
| `build-11-relocation-semantic-corrected` | 0 | 1 | 14,848 | `CC70A8B04DD54A14E0CBBF59C7EA1B6F7969C4F747DBF47DC735CB870EF013BB` |
| `build-12-relocation-semantic-corrected` | 0 | 1 | 14,848 | `CC70A8B04DD54A14E0CBBF59C7EA1B6F7969C4F747DBF47DC735CB870EF013BB` |

The DLLs are byte-identical. Assembly name is
`reloc`, version `0.0.0.0`,
MVID `9262b9fc-ad70-4148-82fc-c8dcf16f7265`.

Mono.Cecil inspection found 223 calls and 102 distinct call signatures. Reviewed
counts are:

- `SemanticSnapshot.Capture`: 2;
- `EntityBuffs.AddCustomVar`: 2;
- `SpawnPointList.GetRandomSpawnPosition`: 1;
- `Entity.SetPosition`: 1;
- `World.CanPlayersSpawnAtPos`: 2;
- `Vector3.Distance`: 1; and
- direct `EntityBuffs.SetCustomVarNetwork`: 0.

Both `RELOC_SEMANTIC_UNCHANGED` and `RELOC_SEMANTIC_CHANGED` are compiled in.
There are zero reviewed Harmony, file/directory mutation, process-launch,
teleport, respawn, or marker-removal calls. Direct assembly references are
limited to `Assembly-CSharp`, `LogLibrary`, `mscorlib`, `System.Core`, and
`UnityEngine.CoreModule`.

Microsoft Defender platform `4.18.26070.9`, signature `1.457.265.0`, scanned
both DLLs individually and found no threats with exit code `0`.

No payload was staged, no live Mod or save was changed, and the artifact was not
executed. Payload staging requires separate owner authorization.
