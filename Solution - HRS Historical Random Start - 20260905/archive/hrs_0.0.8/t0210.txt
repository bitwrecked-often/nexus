BIT WRECKED HISTORICAL RANDOM START - ALPHA 6 METHOD
====================================================

DOUBLE-CLICK THIS FILE TO READ THE PROJECT STATUS.

To open the current design-only Bit Wrecked window, double-click:

    r0136.bat

Current status:

    Phase 0 discovery complete
    Phase 0A pre-build packet prepared for review
    Pure contract tests passed; game-runtime tests have not started
    Random-start safeguard scopes are preview-only and default off
    Starting-biome-only and all-dangerous-biomes stay editable
    Nearest opening-trader guidance is preview-only
    No runtime helper
    No gameplay payload
    No policy-saving action
    No installer or remover

The BAT file opens a read-only framework window. It can recognize a selected
7 Days to Die folder and preview `Starting biome only` or `All dangerous
biomes` safeguards for Random starts. Both begin unchecked, cannot both be
selected, and remain editable under either start type; Standard ignores them.
It also explains the
planned nearest-trader handling for the opening stone-shovel quest, but it
cannot alter the game, save a spawn policy, move a player, change a quest,
install a mod, or publish anything.

Full document routing begins in n0209.md.
