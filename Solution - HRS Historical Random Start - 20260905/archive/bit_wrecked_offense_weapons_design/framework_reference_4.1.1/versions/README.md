# Versioned Solution Lanes

This solution owns its version lanes here.

- `4.0.1/` is the preserved historical DEV copy.
- `4.1.0/` is the prior 4.1.x GUI review boundary.
- `4.1.1/` is the active GUI clarity review boundary.

The parent solution remains the working source until routing and release tools
are migrated. Keep the external Nexus repository's matching version structure
in sync only through deliberate reviewed copies.
