# Historical Random Start 0.0.5 — Version Identity Inventory

Date: 2026-08-26  
Status: ACTIVE-LANE VERSION DECISION

## Approved Mapping

| Surface | Approved value | Reason |
| --- | --- | --- |
| Development lane and release metadata | `0.0.5-alpha-tech-demo` | Honest prerelease identity for the active technical-demonstration lane. |
| Game `ModInfo.xml` | `0.0.5` | Numeric three-part game-facing version compatible with the existing metadata form. |
| Policy/result/deployment/history/recovery schemas | Existing `v1` values | Product version advancement does not change these contracts. |
| Frozen visual preview | `0.0.7-preview` | Separate historical UI-preview chronology; not the runtime release identity. |

## Current Identity Changes

| Location | Prior value | New value | Classification |
| --- | --- | --- | --- |
| `ui/p0158.ps1` version label | `0.0.1-alpha` | `0.0.5-alpha-tech-demo` | Current manager identity. |
| Same manager's deployment-manifest argument | `0.0.1-alpha` | `0.0.5-alpha-tech-demo` | Current deployment metadata. |
| `src/runtime/main/ModInfo.xml` | `0.0.1` | `0.0.5` | Current game-facing runtime metadata. |
| `src/runtime/main/j0219.json` | `0.0.1-alpha` | `0.0.5-alpha-tech-demo` | Current release-source identity. |
| Current front-door and routing documents | unversioned/`0.0.1` working lane | dated `0.0.5` active lane | Current authority routing. |

The release-source manifest's `ModInfo.xml` entry and aggregate must be
regenerated after the metadata change. No C# behavioral source is changed by
the version conversion.

## Intentionally Preserved Values

| Value or location | Decision |
| --- | --- |
| Filenames ending `_0.0.1.md` | Preserve. They are dated contract/evidence versions, not current product metadata. |
| `n0138.md` and schema descriptions | Preserve until the contract itself changes. |
| `policy.v1.json`, `result.v1.json`, `hrs-*/v1` schemas | Preserve. No schema revision is authorized or required. |
| `tests/p0169.ps1` values such as `0.0.1-alpha.1` | Preserve as generic valid prerelease grammar fixtures; they do not declare the current product version. |
| Phase 1 and Phase 1A probe `ModInfo.xml` and `j0219.json` versions | Preserve as historical private proof-instrument identities. |
| `src/runtime/j0199.json` | Preserve as the historical pinned proof dependency record. |
| `out/` and `tmp/` metadata/binaries | Preserve as historical generated evidence; do not relabel old artifacts as `0.0.5`. |
| `p0159.ps1` and `0.0.7-preview` | Preserve as the frozen visual-only preview. |
| Changelog references to preview `0.0.2` through `0.0.7` | Preserve as chronology. |
| Wasteland `4.1.1` references | Preserve as external framework/process references. |

## Prohibited Conversion Methods

- No blind global replacement of `0.0.1`.
- No renaming of historical evidence to imply it was produced for `0.0.5`.
- No schema-version bump without an actual contract change and migration plan.
- No binary or build-record relabeling.
- No change to the frozen Nexus-review lane.
